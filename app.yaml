runtime: python312
instance_class: F1

handlers:
  # Static assets with caching
  - url: /assets
    static_dir: dist/assets
    expiration: "30d"

  # All other routes go to index.html (SPA support)
  - url: /.*
    static_files: dist/index.html
    upload: dist/index.html
